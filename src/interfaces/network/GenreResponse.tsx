import { Genre } from "../models/Genre";

/**
 * Genre response interface and corresponding Genre data interface
 *  for handling data calls to the back endpoints
 */
 export interface GenreResponse {
     data: Genre
 }